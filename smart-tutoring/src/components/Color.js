import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
        light: '#FF453C',
        main: '#FF453C',
        dark: '#FF453C',
        contrastText: '#fff',
    },
    secondary: {
        light: '#FF453C',
        main: '#FF453C',
        dark: '#FF453C',
        contrastText: '#000',
    }
  }
});

export default theme;




/*import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#FF453C',
      main: '#FF453C',
      dark: '#FF453C',
      contrastText: '#fff',
    },
    secondary: {
      light: '#FF453C',
      main: '#FF453C',
      dark: '#FF453C',
      contrastText: '#000',
    },
  },
});*/
